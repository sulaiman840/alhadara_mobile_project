<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- DEBUG ONLY: allow HTTP (cleartext) for every host -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
